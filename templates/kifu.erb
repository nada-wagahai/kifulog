<html>
<head>
<meta charset="UTF-8">
<title><%= kifu.start_time %></title>
</head>
<body>
  <dl>
  <dt>開始日時</dt><dd><%= kifu.start_time %></dd>
  <dt>終了日時</dt><dd><%= kifu.end_time %></dd>
  <dt>手割合</dt><dd><%= kifu.handicap %></dd>
  <dt>棋戦</dt><dd><%= kifu.game_name %></dd>
  <dt>先手</dt><dd><%= kifu.first_players.map {|p| p.masked_name}.join(", ") %></dd>
  <dt>後手</dt><dd><%= kifu.second_players.map {|p| p.masked_name}.join(", ") %></dd>
  </dl>
  <table>
    <tr><th>手数</th><th>指手</th><th>消費時間</th><th></th></tr>
    <tr>
      <td></td>
      <td>対局開始</td>
      <td></td>
      <td><a href="0">■</a></td>
    </tr>
    <% kifu.steps.each do |step| %>
      <tr>
        <td><%= step.seq %></td>
        <td><%= step.move %></td>
        <td><%= step.elapsed_time %></td>
        <td><a href="<%= step.seq %>">■</a></td>
      </tr>
    <% end %>
  </table>
  <a href="../../admin/download/<%= kifu.kifu_id %>">元データ</a>
  <a href="../..">棋譜一覧</a>
</body>
</html>
